{
  "name": "@zippy/api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "lint": "eslint . --max-warnings=0 --no-error-on-unmatched-pattern",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "build": "echo 'No build configured for @zippy/api yet'",
    "dev": "echo 'No dev server configured for @zippy/api yet'"
  }
}
